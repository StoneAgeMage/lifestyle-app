// ============================================================
// MEALS — initialises the meal planner in the Fuel tab
// Phase 7: rotation grid removed; all static content is in HTML.
// ============================================================

function renderMeals() {
  if (typeof initMealPlanner === 'function') initMealPlanner();
}
