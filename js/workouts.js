// Workout reference removed in Phase 9.
// Day-specific workout details now load on tap via the Plan tab modal.
// renderWorkouts() stub kept so any cached references don't throw.
function renderWorkouts() {}
